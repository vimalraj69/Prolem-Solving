class Solution {
    public int commonFactors(int a, int b) {
       int n=Math.min(a,b);
       int count=0;
       while(n>0){
        if(a%n==0 && b%n==0){
            count++;
        }
        n--;
       } 
       return count;
    }
}
