class Solution {
    public int averageValue(int[] nums) {
        int ans=0;
        int c=0;
        for(int i : nums){
            if(i%2==0 && i%3==0){
                ans+=i;
                c++;
            }
        }
        if(c>0)
         ans=ans/c;
        return ans;
    }
    
}
